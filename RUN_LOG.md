# Overnight Run Log

Cumulative spend: $0.00369 / $2.00 cap
Wallet start: ~$4.75 (confirmed $4.750418 pre-run via /v1/accounts)

| time | item | status | cost | cumulative | output | notes |
|---|---|---|---|---|---|---|
| 2026-07-04 03:48:40 -0700 | Item 1: fix + re-run service_sweep (images, compute) | done | $0.003690 | $0.003690 | dryrun/service_sweep_result.json | TONIGHT's spend = $0.003690 only (images+compute). Fal images path was `${host}/v1/run/fal-ai/flux/schnell` (404) — probed unauthenticated (404=no route, 402=route exists=needs payment) and found correct path drops the `/v1/` prefix: `https://fal.services.sapiom.ai/run/fal-ai/flux/schnell` → 200 ($0.003). Blaxel compute host `compute.services.sapiom.ai` doesn't resolve in DNS at all (confirmed via dig/curl, not just a wrong path) — correct host is `blaxel.services.sapiom.ai` + singular path `/v1/run` (not `/v1/runs`) → 200 ($0.00069). Added `--only=svc1,svc2` flag to service_sweep.js so re-fires merge into the existing 9-service result file instead of re-spending on the 7 services that already worked from the PRIOR (pre-overnight) run: search $0.006, llm $0.0001, audio $0.001, scraping $0.009 — those are historical/already-spent, not tonight's cost. data=$0 (documented free endpoint), messaging+verify SKIPPED by design (side-effecting, real SMS/webhook). Balance $4.750418 → $4.746728 (tonight only). |
| 2026-07-04 04:05:00 -0700 | Item 2: BUILD 0 — extend ingest.py schema | done | $0.00 | $0.003690 | spend.duckdb (transactions.trace_external_id, costs.cost_details), ingest.py | Free (GET-only). PROBED live GET /v1/transactions first (single-cost txn + a 2-cost/superseded txn) to confirm field availability, matching BACKLOG BUILD 0 exactly: `trace.externalId` present (always null — never set until BUILD 3 chaining), `outcome` top-level present (values success/error), `costs[].isEstimate/supersededAt/supersedesCostId/costDetails` all present (costDetails shape varies: hold rows `{source:payment_transaction,...}`, settlement rows `{source:payment_capture, capturedAmount, originalAmount}`). CONFIRMED ABSENT (checked both txn shapes): no top-level `payment` sub-object, no `factPhase` field anywhere — per instructions did NOT add either. Added only `transactions.trace_external_id` and `costs.cost_details` (JSON). Hit and fixed a real bug during this build: ALTER TABLE ADD COLUMN (for migrating the pre-existing db) appends new columns at the physical end of the table, which broke the old positional `INSERT ... VALUES (?,?,...)` (values landed in wrong columns — a timestamp got shoved into the JSON `raw` column, threw a Conversion Error). Fixed by switching both INSERT statements to explicit named columns. Caught before any bad data was committed (DuckDB executemany rolled the batch back on error) — restored from a pre-run backup as a precaution, verified backup was clean (50/61 rows, matches post-fix baseline), then re-ran. Verified on fixture first (6 txns/8 costs, ran twice, identical counts) before touching real db. Real re-ingest: 75 transactions / 98 cost rows, ran twice, identical counts both times (idempotency proof). trace_external_id is NULL on all 75 (expected — no chaining fired yet). cost_details populated on all 98 cost rows. outcome: 73 success / 2 error. Also re-ran audit.py to refresh report.md against the now-current 75-txn dataset (was stale at 50 txns) — all 4 checks still pass except 4b (3 completed txns w/ zero cost rows: the pre-fix Fal 404 + pre-fix Blaxel fetch-fail + the documented-free Neon price call — expected, not a bug). |
| 2026-07-04 04:20:00 -0700 | Item 3: BUILD 2 — findings.py bundle | done | $0.00 | $0.003690 | findings.py, findings.md | Free (spend.duckdb only, no network). Built all 6 sections from BACKLOG BUILD 2. (1) Settlement latency per service — flagged+explained a real quirk: flat single-row services show *negative* latency because the cost row is created slightly before `authorizedAt` is stamped (authorization-step artifact, not a bug); only sapiom_openrouter (chained) shows genuine positive settlement wait (p50 5.75s/p95 12.24s). (2) x402 tax = completedAt-authorizedAt per HANDOFF (no payment sub-object exists, confirmed in BUILD 0). (3) Cost-per-task: EMPTY as expected (0/75 txns have trace_external_id) — noted, not failed; will populate after BUILD 3. (4) Estimate-accuracy: sapiom_openrouter avg settled/held ratio 0.360 (holds overestimate real cost by ~2.8x) — the flat-max_tokens-pricing finding, now quantified per-service. (5) Runaway detection: peer-relative statistical flag (median gap < 20% of peer median), not a fixed-seconds threshold. Real finding: the agent literally named `spend-runaway` did NOT show a burst in tonight's data (steady ~8s gaps) — `fleet-test`, an unrelated older test agent, was the actual anomaly (10 calls in <1s, ~80ms gaps) and got correctly flagged RUNAWAY. (6) callSite lineage: probed, confirmed absent on all captured transactions, section notes this and stops. |
| 2026-07-04 04:35:00 -0700 | Item 4: BUILD 1 — CEO dashboard | done | $0.00 | $0.003690 | export_dashboard.py, dashboard.html, dashboard_data.json, dashboard_data.js | Free (spend.duckdb + dryrun/extrapolation_result.json only, no network calls during build; 2 WebSearches done for public prices, no API spend). Built the 5-tile "demo cut" per BACKLOG (panels 1,3,4,5,2): float meter, capital-efficiency ratio, reconciliation health, runaway detection, take-rate/margin. export_dashboard.py imports audit.py's and findings.py's own check functions directly (importlib) rather than re-deriving SQL, so dashboard numbers are guaranteed identical to report.md/findings.md — single source of truth per metric. WebSearched public prices (2026-07-04): OpenRouter gpt-4o-mini $0.15/1M in, $0.60/1M out (openrouter.ai/openai/gpt-4o-mini); Linkup standard-depth search ~$0.005/call (docs.linkup.so pricing page, EUR5/1000 queries). Computed: Linkup markup +20% (Sapiom $0.006 vs public $0.005, apples-to-apples flat pricing); OpenRouter noted as a data-completeness gap (no token counts in the ledger, so exact per-token markup can't be computed) but observed Sapiom's HOLD rate ($0.0006/1k max_tokens) matches OpenRouter's public OUTPUT rate almost exactly — hold prices every token as a worst-case output token. DESIGN DECISION: dashboard.html loads data via `<script src="dashboard_data.js">` (a JS-wrapped copy export_dashboard.py also writes), NOT `fetch('dashboard_data.json')` — fetch() of a local file is blocked by Chrome's CORS policy under file://, which would break "opens with no network/server" on a plain double-click. VERIFIED OFFLINE: ran headless Google Chrome (`--headless --dump-dom`) directly against `file://.../dashboard.html` with no server — all 5 tiles rendered with real computed numbers, zero console errors. Confirmed numbers match: reconciliation diff $0.000000 (== report.md), naive overstatement +10.8% (== report.md's +10.77%), runaway flagged = fleet-test (== findings.md), float ratio 4.4× (== extrapolation_result.json Exp B). Runaway agent row rendered with the red `flag-red` CSS class as required by acceptance criteria. |
